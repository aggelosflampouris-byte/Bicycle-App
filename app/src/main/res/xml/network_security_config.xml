<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security config:
    - Allows cleartext (HTTP) traffic to tile.openstreetmap.org for map tiles,
      since some OSM tile mirrors serve over HTTP.
    - All other traffic (HF API, etc.) goes over HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">tile.openstreetmap.org</domain>
        <domain includeSubdomains="true">openstreetmap.org</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
